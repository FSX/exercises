package astar

import (
	// "reflect"
	// "strings"
	"testing"
)

func TestSquareGrid(t *testing.T) {
	// graph := NewSquareGrid(50, 20)
	// graph.AddWall(20, 5, 1, 5)
	// graph.AddWall(30, 8, 1, 5)
	// t.Logf("\n%s", graph)
}
